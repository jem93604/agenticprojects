# Dev / CI only — install after `pip install -r requirements.txt` in the same venv.
pytest>=8.0
# Starlette/FastAPI TestClient passes app= into httpx.Client; httpx 0.28+ removed it.
httpx>=0.27,<0.28
pytest-cov>=4.0
